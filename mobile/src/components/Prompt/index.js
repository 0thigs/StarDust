import { Button } from '../Button';
import { Terminal } from 'react-native-feather';
import theme from '../../global/styles/theme';
import * as C from './styles';

export function Prompt({ isVisible, title, value, setter, onConfirm, onCancel }) {
  return (
      <C.Content>
       
      
      </C.Content>
  );
}
